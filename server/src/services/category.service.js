// import categoryModel from "../models/category.model.js";

// const createCategory = async ({ name, color, icon, type }) => {
//     try {

//         if (categoryModel.findOne({ name: name })) { 
//             throw new Error("Category name already exists");
//         }

//         const newCategory = await categoryModel.create({
//             name: name,
//             color: color,
//             icon: icon,
//             type: type,
//         })

//         return {
            
//         }

//     } catch (error) {

//     }
// }